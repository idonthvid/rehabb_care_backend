parters:


function doPost(e) {
  try {
    Logger.log('Received POST request');
    Logger.log('Post data: ' + e.postData.contents);
    
    const sheet = SpreadsheetApp.getActiveSpreadsheet().getSheets()[0];
    
    if (!sheet) {
      throw new Error('No sheet found');
    }
    
    Logger.log('Sheet name: ' + sheet.getName());
    
    const data = JSON.parse(e.postData.contents);
    Logger.log('Parsed data: ' + JSON.stringify(data));
    
    const row = [
      new Date(),
      data.name || '',
      data.phone || '',
      data.email || '',
      data.specialization || '',
      data.pincode || '',
      data.city || '',
      data.referralCode || ''
    ];
    
    Logger.log('Adding row: ' + JSON.stringify(row));
    sheet.appendRow(row);
    Logger.log('Row added successfully');
    
    return ContentService.createTextOutput(JSON.stringify({
      status: 'success',
      message: 'Partner application saved successfully'
    })).setMimeType(ContentService.MimeType.JSON);
    
  } catch (error) {
    Logger.log('Error: ' + error.toString());
    return ContentService.createTextOutput(JSON.stringify({
      status: 'error',
      message: error.toString()
    })).setMimeType(ContentService.MimeType.JSON);
  }
}

function doGet(e) {
  return ContentService.createTextOutput('Partner API is running');
}




















appointment:

function doPost(e) {
  try {
    Logger.log('Received POST request');
    
    // Handle undefined event object (for testing)
    if (!e) {
      Logger.log('Event object is undefined - using test data');
      e = {
        postData: {
          contents: JSON.stringify({
            name: 'Test User',
            phone: '1234567890',
            email: 'test@example.com',
            serviceArea: 'Test Service',
            preferredDateTime: '2025-01-01 10:00',
            city: 'Test City',
            pincode: '110001',
            address: 'Test Address',
            notes: 'Test Notes'
          })
        }
      };
    }
    
    Logger.log('Event object: ' + JSON.stringify(e));
    
    // Check if postData exists
    if (!e.postData || !e.postData.contents) {
      throw new Error('No post data received');
    }
    
    Logger.log('Post data: ' + e.postData.contents);
    
    // Get the first sheet (index 0) instead of by name
    const sheet = SpreadsheetApp.getActiveSpreadsheet().getSheets()[0];
    
    if (!sheet) {
      throw new Error('No sheet found');
    }
    
    Logger.log('Sheet name: ' + sheet.getName());
    
    const data = JSON.parse(e.postData.contents);
    Logger.log('Parsed data: ' + JSON.stringify(data));
    
    // Check if it's a consultation (has 'location' field but no 'email')
    const isConsultation = data.location && !data.email;
    
    // Add timestamp and form data to sheet
    const row = isConsultation ? [
      new Date(),
      data.name || '',
      data.phone || '',
      '-',  // email
      '-',  // serviceArea
      '-',  // preferredDateTime
      data.location || '',  // city/location
      '-',  // pincode
      '-',  // address
      '-'   // notes
    ] : [
      new Date(),
      data.name || '',
      data.phone || '',
      data.email || '',
      data.serviceArea || '',
      data.preferredDateTime || '',
      data.city || '',
      data.pincode || '',
      data.address || '',
      data.notes || ''
    ];
    
    Logger.log('Adding row: ' + JSON.stringify(row));
    sheet.appendRow(row);
    Logger.log('Row added successfully');
    
    return ContentService.createTextOutput(JSON.stringify({
      status: 'success',
      message: isConsultation ? 'Consultation saved successfully' : 'Appointment saved successfully'
    })).setMimeType(ContentService.MimeType.JSON);
    
  } catch (error) {
    Logger.log('Error: ' + error.toString());
    return ContentService.createTextOutput(JSON.stringify({
      status: 'error',
      message: error.toString()
    })).setMimeType(ContentService.MimeType.JSON);
  }
}

function doGet(e) {
  return ContentService.createTextOutput('Appointment API is running');
}

// Test function - Run this to test the script
function testAppointment() {
  Logger.log('Starting test...');
  const result = doPost();
  Logger.log('Test result: ' + result.getContent());
}

// Test function for consultation
function testConsultation() {
  Logger.log('Starting consultation test...');
  const e = {
    postData: {
      contents: JSON.stringify({
        name: 'Test Consultation User',
        phone: '9876543210',
        location: 'Bangalore'
      })
    }
  };
  const result = doPost(e);
  Logger.log('Test result: ' + result.getContent());
}

// Direct test function - adds a row directly
function testDirectAdd() {
  try {
    Logger.log('Starting direct test...');
    const sheet = SpreadsheetApp.getActiveSpreadsheet().getSheets()[0];
    Logger.log('Sheet found: ' + (sheet ? 'Yes' : 'No'));
    Logger.log('Sheet name: ' + sheet.getName());
    
    if (sheet) {
      const row = [
        new Date(),
        'Test User',
        '1234567890',
        'test@example.com',
        'Knee & Ankle Care',
        '2025-01-15 10:00',
        'Mumbai',
        '400001',
        'Test Address 123',
        'Test notes here'
      ];
      sheet.appendRow(row);
      Logger.log('Test row added successfully!');
    }
  } catch (error) {
    Logger.log('Error: ' + error.toString());
  }
}



















consultaions:


function doPost(e) {
  try {
    Logger.log('Received POST request');
    Logger.log('Event object: ' + JSON.stringify(e));
    
    // Check if postData exists
    if (!e || !e.postData || !e.postData.contents) {
      throw new Error('No post data received');
    }
    
    Logger.log('Post data: ' + e.postData.contents);
    
    const sheet = SpreadsheetApp.getActiveSpreadsheet().getSheets()[0];
    
    if (!sheet) {
      throw new Error('No sheet found');
    }
    
    Logger.log('Sheet name: ' + sheet.getName());
    
    const data = JSON.parse(e.postData.contents);
    Logger.log('Parsed data: ' + JSON.stringify(data));
    
    const row = [
      new Date(),
      data.name || '',
      data.phone || '',
      data.location || ''
    ];
    
    Logger.log('Adding row: ' + JSON.stringify(row));
    sheet.appendRow(row);
    Logger.log('Row added successfully');
    
    return ContentService.createTextOutput(JSON.stringify({
      status: 'success',
      message: 'Consultation saved successfully'
    })).setMimeType(ContentService.MimeType.JSON);
    
  } catch (error) {
    Logger.log('Error: ' + error.toString());
    return ContentService.createTextOutput(JSON.stringify({
      status: 'error',
      message: error.toString()
    })).setMimeType(ContentService.MimeType.JSON);
  }
}

function doGet(e) {
  return ContentService.createTextOutput('Consultation API is running');
}
